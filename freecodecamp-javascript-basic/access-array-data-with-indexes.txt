/*
Akses Data Array dengan Indeks

Kita dapat mengakses data di dalam array menggunakan indexes .

Indeks array ditulis dalam notasi braket yang sama dengan yang digunakan string, kecuali bahwa alih-alih menentukan karakter, mereka menentukan entri dalam array. Seperti string, array menggunakan pengindeksan berbasis nol , sehingga elemen pertama dalam array memiliki indeks 0.


Contoh

const array = [50, 60, 70];
console.log(array[0]);
const data = array[1];
console.log(array[0])Mencetak , 50dan datamemiliki nilai 60.

-> Buat variabel yang disebut myDatadan atur agar sama dengan nilai pertama myArraymenggunakan notasi braket.

Tes
Menunggu :Variabel myDataharus sama dengan nilai pertama dari myArray.
Menunggu :Data dalam variabel myArrayharus diakses menggunakan notasi braket.
 */

const myArray = [50, 60, 70];
const myData = myArray[0];

